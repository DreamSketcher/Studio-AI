# ai_studio_core — обязательные зависимости (headless-ядро, без GUI, без ML).
# Тяжёлые ML-зависимости (torch, torchaudio, TTS, llama-cpp-python, rvc-python)
# ставятся отдельно в зависимости от выбранного набора модулей и/или через
# встроенный менеджер установки ai_studio_core.env_core.
numpy>=1.24
soundfile>=0.12
pydub>=0.25
cryptography>=41.0
certifi>=2023.7
num2words>=0.5
psutil>=5.9
py-cpuinfo>=9.0
packaging>=23.0
PyYAML>=6.0
requests>=2.31
loguru>=0.7
